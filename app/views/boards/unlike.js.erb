$("#like").html("<%= @board.b_like %>");
$("#like_button").html('<p><div class = "thumbs"><%= link_to "추천",  like_path(:id => @board.id), 
                method: :patch, 
                remote: true, class:"glyphicon glyphicon-thumbs-up"%> <p><br><p></div>');